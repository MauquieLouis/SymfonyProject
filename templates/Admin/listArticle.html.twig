{% extends 'base.html.twig' %}

{% block title %}Les maçons de L'informatique{% endblock %}

{% block body %}

	<div class ="img7">
	<div class ="contenuTitre">
		<h3>List Article</h3>
	</div>
		<ul>
		    {% for article in articles %}
				<li>{{ article.title }} : {{ article.author }} : {{ article.publishedAt ? 'published' : 'not published'}} <a href ="{{ path('admin_article_edit',{'id': article.id}) }}">Edit Article</a></li>
				</br>
		    {% endfor %}
		</ul>
	</div>
{% endblock %}